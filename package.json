{
  "name": "proxy",
  "version": "1.0.0",
  "description": "Intermediate server that acts as an intermediary between the user and the target server",
  "main": "src/server.js",
  "type": "module",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start_server": "node --watch src/server.js"
  },
  "author": "a.asenchik",
  "license": "ISC",
  "dependencies": {
    "@joi/date": "^2.1.0",
    "axios": "^1.6.7",
    "date-fns": "^3.3.1",
    "dotenv": "^16.4.5",
    "express": "^4.18.3",
    "joi": "^17.12.2"
  }
}
